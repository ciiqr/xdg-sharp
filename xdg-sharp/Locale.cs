using System;

namespace xdg
{
    public class Locale
    {
        // TODO: Finish implementing

        // TODO: Validate this regex
        public static string regex = @"(\[([a-zA-Z]+)(_[a-zA-Z]+)?(\.[a-zA-Z\-0-9]+)?(@[a-zA-Z]+)?\])?";
        public static string[] langs = {}; // TODO: Actually implement this properly
        public Locale()
        {
        }
    }
}

